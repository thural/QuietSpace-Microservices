spring:

  application:
    name: notification-service

  config:
    import: optional:configserver:${CONFIG_URI:http://localhost:8899/config-server}
  profiles:
    active:
      - ${CONFIG_ACTIVE_PROFILE}

  data:
    mongodb:
      database: ${DB_NAME}
      uri: ${NOTIFICATION_DB_URI}
      replica-set-name: ${MONGO_REPLICASET_NAME}

  kafka:
    bootstrap-servers: http://${KAFKA_BROKER_HOST:localhost}:${KAFKA_BROKER_PORT:9092}
    template:
      default-topic: notificationTopic
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring:
          json:
            type:
              mapping: message_sent_event:com.jellybrains.quietspace_backend_ms.notification_service.event.MessageSentEvent
      group-id: notificationId

eureka:
  client:
    serviceUrl:
      defaultZone: ${DISCOVERY_SERVER_URI:http://localhost:8761/eureka}

server:
  port: ${NOTIFICATION_SERVICE_PORT}
